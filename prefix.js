const prefix = function (strs) {
  // check for invalid argument
  // find the max prefix length
  // make a loop based on max prefix
  // find the first element of strs
  // compare others with first element
};

console.log(prefix(["flower", "flow", "flight"]));
console.log(prefix(["dog", "racecar", "car"]));

console.log(prefix(["flower", "apple", "app"]));
